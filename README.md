# 简迹 CV

简迹 CV 是一个用 AI 辅助用户完成简历的目标驱动平台。留学申请与毕业求职不是两套产品，而是新建简历时选择的两种用途：

- 留学申请：根据大学、地区与申请方向组织教育、研究和项目证据。
- 毕业求职：根据国企、大厂与岗位方向突出能力、业务影响和关键词连接。

项目采用 clean-room 方式实现：研究了 Resume.com 的公开信息架构与常见简历产品流程，但未复制其商标、受版权保护的文案、图片、模板、源码或不可见的后端架构。

## 当前可用能力

- 凝练的 AI 简历首页，以及合并“新建”和“工作台”的单一入口
- 186 个目标适配方案：101 所院校、85 家企业，按地区或企业类型二级选择
- 16 套原创专业版式（留学与求职各 8 套），覆盖学术研究、国际申请、金融咨询、科技产品、工程制造、央国企等 8 类真实阅读情境；创建时展示缩略图、推荐理由与非官方声明
- 目标搜索、自定义目标、场景标签与非官方编辑建议说明
- 服务端简历 CRUD、软删除与用户级资源隔离
- D1/SQLite 持久化、修订快照和审计事件
- 六大简历章节编辑、实时 A4 预览、自动保存
- 求职场景支持独立保存目标岗位、单个 JD、来源标签/链接与记录日期，并生成“岗位要求 → 用户原文证据 → 缺口 → 撰写动作”的确定性证据地图
- 用户可从证据地图选择一条岗位要求和一条可定位原文，获得结构化改写草稿、修改理由、引用证据与待补事实；确认后精确替换原文并可撤销，不再向经历中追加无来源草稿
- 服务端 OpenAI Responses API 适配层、结构化输出校验、逐次同意记录、最小化模型输入，以及会话 / 网络 / 全局配额、单会话单请求和四路全局并发闸门；模型返回的原文定位、新数字和工具词还需通过本地事实安全门，未配置模型或调用失败时透明降级为基础分析
- 编辑器内统一完成打印 / 存为 PDF、ATS 纯文本和网页简历输出
- 单文件网页简历默认隐藏联系方式，并使用严格 CSP/HTML 转义
- 根据当前目标与证据缺口，在填写区下方最多展示一条可选学习提示；来源包括中国大学 MOOC、Khan Academy、Coursera、edX、Microsoft Learn 等官方页面
- 我的简历：新建、搜索、筛选、重命名、复制与删除草稿
- 演示管理台：指标、赛道分布、内容治理和健康检查
- 响应式布局、键盘焦点、减少动态效果与打印样式

## 本地运行

环境要求：Node.js 22.13 或更高版本。

```bash
npm install
npm run dev
```

开发服务器会输出实际本地地址。D1 在本地由 Cloudflare Miniflare 提供，首次 API 请求会自动创建表并写入种子数据。

## 质量检查

```bash
npm run typecheck
npm run lint
npm test
npm run build
```

数据库结构定义保存在 `db/schema.ts`，已生成并审查的初始迁移保存在 `drizzle/`。

## 数据与安全说明

- 当前版本为浏览器级私有访客空间：随机 HttpOnly 会话令牌映射到独立用户，所有简历 API 都在服务端校验资源所有权。
- 清除浏览器数据后访客可能无法再次访问原草稿；访客空间创建、基础建议和模型建议均有网络或全局用量上限。生产接入真实用户前仍应启用账号认证、管理员 MFA、数据保留和账号删除流程。
- 默认基础分析不会调用第三方模型，也不会自动虚构成绩、工作经历或量化成果；CV 正文会发送到本平台 API 进行检查与保存。
- 只有服务端同时配置 `OPENAI_API_KEY` 与 `OPENAI_MODEL`，且用户在编辑器中主动勾选后，当前请求才会发送给 OpenAI Responses API；系统会记录同意版本、用途、目标简历与时间，不记录同意时的 CV 正文。
- 模型请求使用结构化输出并设置 `store: false`；概览分析通过显式字段投影排除姓名、邮箱、电话、教育/经历所在地和项目链接，所有自由文本先做常见招聘联系方式脱敏，JD 只发送优先提取的最多 12 项要求。可应用草稿必须引用当前简历中的稳定原文位置；原文变化、伪造位置、新增数字或新增常见工具词会被拦截，结果仍须逐条核实。服务商的数据处理仍以其协议与账号配置为准。
- 岗位分析只以用户粘贴并确认的单个 JD 文字为准；BOSS、智联等第三方链接仅作为可选出处记录，系统不自动打开、调用平台 API 或批量抓取。当前版本不上传岗位截图，用户可先用设备文字识别后粘贴；JD 不进入公共岗位库或训练语料。
- 真实用户上线前仍需完成个人信息保护法/GDPR 专项评估、服务提供方协议审查和撤回同意流程。
- 产品管理员不能获得服务器 Shell 或宿主电脑控制权；产品角色与云端运维 IAM 必须隔离。

## 目录

```text
app/                    页面与 API Route Handlers
db/                     D1/SQLite schema 与初始化
drizzle/                数据库迁移
src/components/         共享 UI 与简历渲染器
src/lib/                校验、建议、数据映射与种子目录
src/types/              领域类型
tests/                  核心逻辑单元测试
docs/                   架构与接口说明
```

## 后续路线图

1. 真实身份认证、访客草稿迁移和多工作区权限。
2. 目标画像 CMS、企业官方 ATS / 授权数据源、来源复核、发布流程与有效期提醒。
3. 上传 PDF/DOCX、字段映射、OCR 与原文件独立删除。
4. 服务端 PDF/DOCX 渲染 Worker 与导出任务队列。
5. 可配置模型预算、敏感信息预检、同意撤回和供应商可观测性。
6. 版本差异、目标分叉、导师评论与协作。
7. 可选的静态托管发布集成；当前版本坚持无账号权限的下载即用导出。

详细设计见 [架构说明](docs/architecture.md) 与 [API 说明](docs/api.md)。
