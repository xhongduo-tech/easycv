# Canonical URL used by Open Graph and social sharing metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional future AI provider. The current project ships with deterministic
# first-party rules, so the complete product works without external credentials.
AI_PROVIDER=rules
AI_API_KEY=
